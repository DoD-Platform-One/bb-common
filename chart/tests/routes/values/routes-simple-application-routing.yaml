routes:
  inbound:
    webapp:
      enabled: true
      gateways:
        - istio-gateway/public-ingressgateway
      hosts:
        - webapp.company.com
      service: webapp
      port: 8080
      selector:
        app.kubernetes.io/name: webapp